[tool.poetry]
name = "quant"
version = "0.1.0"
description = ""
authors = ["MagM1go <magmigo2@inbox.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
